############
## Nome e descrição do repositório, modifique para o nome do seu repositório
##
############
REPOSITORY_NAME="Repositório padrão IBICT"
REPOSITORY_DESCRIPTION="Repositório-Padrão para DSpace 7 do IBICT"

############
## Configurações do Backend (serviços do DSpace)
############

# Informe se é http ou https
BACKEND_PROTOCOL=http

# Informe o endereço onde o serviço (backend) do DSpace irá rodar
# Importante, este endereço precisa ser acessível pela interface gráfica.
BACKEND_HOSTNAME=192.169.5.126

# Informe a porta onde o serviço (backend) do DSpace irá rodar
BACKEND_PORT=9080

# Informe "true" caso o repositório use https, "false" caso contrário
BACKEND_USES_SSL=false

############
## Configurações do FrontEnd (interface gráfica do DSpace)
############

# Informe se a interface web será acessada via http ou https
FRONTEND_PROTOCOL=http

# Informe o endereço da interface web
# Importante: este será o endereço que o usuário irá utilizar para acessar o DSpace
FRONTEND_HOSTNAME=192.169.5.126

# Informe a porta da interface web
FRONTEND_PORT=5000

# Informe "true" se a interface web será acessada via https "false" caso o acesso seja via http
FRONTEND_USES_SSL=false

